<h1> Makaleler </h1>
<% unless @articles.empty? %>
  <%  @articles.each do |article|  %>
  Makalenin Adı: <%= link_to article.name, article_path(article)%> </br>
  Etiketler:
  <% article.tags.each do |tag| %>
    <%= tag.name %>
  <% end %> <br>
  Eklenme Tarihi: <%= article.created_at.strftime("%d/%m/%Y") %> <br>
  Makale Sahibi: <%= image_tag article.user.profile.avatar.url, size: 15 %>
  <%= link_to article.user.profile.full_name, profile_path(article.user.profile) %> <br></br>
  <% end %>
<% end %>

